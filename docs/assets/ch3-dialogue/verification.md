# 第 3 章人机对话：本次检查记录

更新日期：2026-09-20。原修订阶段检查和学生本机离线复测的日期均为 2026-09-19，执行主体与证据来源分别记录如下。

**阶段状态：人机对话实验的手册修订、离线检查及学生本机离线复测已完成；本次未配置可用 DashScope Key，在线调用未执行。可先继续后续实验检查，尚未运行的部分保留待验证。**

此状态不表示真实 API、模型推理或设备实验的验收要求已取消，也不代表所有学生应跳过 API 步骤。

## 原修订阶段：Codex 执行的检查（2026-09-19）

以下保留原修订阶段实际执行的检查，不沿用候选包中 2026-09-18 的测试数量或提交状态，也不作为学生复测的环境数据。

### 材料与源码

实际读取了《我们的任务》《协作流程》、用户提供的《具身智能导论-v2.0.pdf》、候选 ZIP 的五个 overlay 文件及七张背景截图。教材核对范围是 PDF 85—97 页（印刷页 67—79），包括截图内的 MiniCPM 示例配置；3.4.4 抓取实验不属于修改范围。

手册基线为 `9f69a377febec5ab5bafffaa2f0e84dec673ce2c`；本次从远端现有 `feature/whr` 克隆、fetch 后切换到该分支，原工作区干净，和 `origin/main` 相同，无需合并。未发现 AGENTS.md 或其他贡献规则文件；已读取 README、README_HANDOOK、MkDocs 配置、文档依赖、工作流及相关源码。部署仅响应 `main/master` 推送或手动触发；本次不调用部署。

只读核对了课程工程提交 `e6bb5d5de828b5d87834ea169b53c09b376d8b09` 的 SenseVoice 示例、依赖与模型代码，以及 CosyVoice 上游提交 `074ca6dc9e80a2f424f1f74b48bdd7d3fea531cc` 的示例、依赖、模型入口和前端。BLIP、Qwen 及百炼用法另与官方资料核对。未修改 `EAI_project`。

### 实际执行的离线检查

运行平台：Windows；Python 3.12.5；MkDocs 1.6.1、Material 9.7.7。文档构建依赖使用隔离虚拟环境。本次没有安装或验证整套模型环境；Python 3.10 检查仅为语法解析，不能记为 Python 3.10 实际运行。

| 检查 | 命令或方法 | 结果 |
|---|---|---|
| Python 语法 | 对两个 `.py` 执行 `py_compile`，缓存写入仓库外 | 通过 |
| Python 3.10 语法 | `ast.parse(..., feature_version=(3, 10))` | 通过，仅语法兼容性 |
| 离线单元测试 | `python -X utf8 docs/assets/ch3-dialogue/test_dialogue_lab.py` | 36 个测试通过 |
| CLI 冒烟 | `--help`；`api --model qwen-turbo --question "离线检查"`，不加 `--send` | 中文帮助/检查输出正常；没有 Key、模型请求或回答文件 |
| 空白与补丁检查 | `git diff --check` | 通过 |
| 页面 Python 代码块 | 提取本页 2 个 Python 代码块，按 Python 3.10 语法解析 | 通过，仅语法检查 |
| 相对链接 | 检查本页及两份配套 Markdown 的 5 个相对链接 | 全部指向实际文件 |
| 生成页面链接 | 检查本页生成 HTML 的本地链接、资源路径和锚点 | 202 项通过 |
| 完整网站 | `python -m mkdocs build --site-dir <系统临时目录>`，原配置与整个仓库 | 基线与修改后均退出 0 |
| 浏览器预览 | 在本机 HTTP 预览中查看首页段落及 ASR 小节、跳转章节锚点 | 中文、表格、代码块、已有图片正常显示；长命令可横向滚动 |

离线测试覆盖消息组装、图像 URI、文本/多模态响应解析、非 200/空回答、文件和端点校验、BOM、输出长度、无 Key、不加 `--send`、输出覆盖保护、异常脱敏，以及模拟 SDK/BLIP/ASR/TTS/摄像头的输入输出与资源释放。Mock 输入与输出都在测试临时目录中；这些测试没有产生真实回答、语音或相机照片。

原修订阶段修复了候选稿中 `.json` 输出被元数据覆盖、输出与输入重合、部分异常未脱敏，以及 SenseVoice 跨目录导入和 Windows 路径分隔符问题。另用上游 FunASR 动态导入函数及一个本地假模块复现 Windows 路径问题：反斜杠路径失败，正斜杠路径通过；此项也不是语音识别实测。

### 网站与三轮复核

构建输出指定到系统临时目录，未改写仓库的 `site/`。原基线完整构建通过；基线没有文档链接 WARNING，存在 Material 工具自身关于未来 MkDocs 2 的提示，不是本次页面引入。

修改后的完整构建通过：新增一条“不在 nav 中”的 INFO 汇总，列出 `run-record-template.md` 与 `verification.md` 两个文件；它们由实验页内链接访问，因此没有修改全站导航配置。新增文档 WARNING 为 0。Material 自身关于未来 MkDocs 2 的提示与基线相同；它不代表本轮使用了 MkDocs 2。

第一轮新生走读修正了录音保存路径及新终端重新配置 Key 的提示；第二轮核对文档参数、源码和证据，未发现阻断提交的问题；第三轮只允许实验页及本目录中四个配套文件进入提交，核对目标为现有 `feature/whr`。没有将文档命令的静态核对写成真实模型成功。

## 学生本机离线复测（2026-09-19）

执行者：学生本人。环境：Windows PowerShell，已激活 `eai-dialogue`。**证据来源：依据学生提供的信息记录；本轮未收到此次复测的终端截图或原始日志。** 原修订阶段提及的七张背景截图不作为此次复测证据。工作目录为手册仓库根目录。

| 实际命令 | 学生反馈的输出 | 验证范围 |
|---|---|---|
| `python docs/assets/ch3-dialogue/dialogue_lab.py --help` | 正常显示 `api`、`caption`、`asr`、`tts`、`capture` 等子命令 | CLI 帮助可显示；不证明各模块依赖或模型已可运行 |
| `python docs/assets/ch3-dialogue/test_dialogue_lab.py` | `Ran 36 tests`，最终为 `OK` | 学生本机的 36 项离线测试通过；Mock 不等于真实 API、模型或设备通过 |
| `python docs/assets/ch3-dialogue/dialogue_lab.py api --model qwen-turbo --question "如何做西红柿鸡蛋？"` | `CHECK ONLY`、文本 API 路线、`Key=未设置` | 未添加 `--send`，未发出真实请求，也未生成真实模型回答 |

未提供此次复测的 Python 完整版本、依赖版本、GPU 配置、耗时或对应提交号；`eai-dialogue` 只是环境名，不能据此推断版本。以上结果由学生执行和反馈，不是 Codex 在本轮重新执行的结果。

## 本轮文档收尾（2026-09-20）

本轮仅补充学生复测与阶段状态，保留原 API 操作步骤、成功判据和下方待验证清单。检查新增文字与学生反馈是否一致，执行 `git diff --check`、相关相对链接检查，并核对最终暂存文件。本轮未重跑单元测试、MkDocs 构建或任何模型与设备实验。

## 尚未完成的真实实验与最小复测

| 路线 | 状态与缺项 | 最小复测步骤 |
|---|---|---|
| A 文本 API | 未运行；需有效 Key、地域、已开通模型及调用预算 | 按手册 3.2 发送单次请求，保存 txt/JSON，核对请求编号与内容 |
| B 图像 API | 未运行；同上，另需允许上传的清晰图片 | 按 4.1 发送一次图文请求；换图对照并人工核对 |
| C BLIP → 文本模型 | 未运行；需完整模型快照、兼容 torch/Transformers、下载额度 | 按 4.2 生成 caption，核对后再传给文本 API |
| 第一代 Qwen 本地 | 未运行；需历史兼容环境、完整快照与确认过的 GPU 资源 | 按 8.1 环境检查后运行三轮对话，保存上下文结果与版本 |
| MiniCPM-V-2.6 | 受阻；缺唯一镜像 ID/版本、完整 run.ipynb、依赖与模型 revision | 教师补齐资料并确认实例预算后，按 8.2 逐格运行、保存 notebook |
| D SenseVoice | 未运行；需语音环境、SenseVoice/VAD 模型、真实录音 | 按 5.2 输出 question.txt，对照录音检查误字漏字 |
| E CosyVoice2 | 未运行；需指定源码、子模块、模型快照、兼容依赖与参考音频 | 按 6.2 合成 answer.txt，并人工播放验收 reply.wav |
| F 摄像头 | 未运行；需设备及使用许可 | 本人确认后按 7.1 采一帧、打开图片核对 |
| 录音 | 未运行；需可用麦克风及使用许可 | 本人确认后按 5.1 保存短录音并播放核对，再交给 ASR |
| 文件级完整交互 | 未运行；缺各阶段的真实成功输出 | 按 7.2 依次传递同轮文件，对照原问题验收最终音频 |
| 实时交互 | 未实现；本页仅补充可排错的文件链路 | 后续另实现实时录音、结束判定、播放和打断后验收 |

没有调用收费 API、下载模型权重、启动摄像头/录音或机器人，也没有训练。辅助审阅和脚本修订使用了 Codex；教材图和已有界面示意未当作本次结果。提交与 PR 的实际状态应以 GitHub 为准，候选包旧记录不能作为提交凭证。
