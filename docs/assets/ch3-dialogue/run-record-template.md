# 人机对话实验运行记录

未执行的步骤填“未运行”，不要用教材输出或离线测试代替模型结果。

## 环境

日期、系统、Python 版本、手册提交、课程/上游源码提交、模型快照 revision、CPU/GPU/显存、驱动版本、实际完成的步骤：

API 地域、端点、实际模型名、请求编号与耗时（不填写 Key）：

采用教材路线还是替代路线？采用文件级还是实时集成？

## 运行结果

| 步骤 | 实际命令及工作目录 | 同轮输入/输出文件、记录时间 | 退出码与人工结果判定 |
|---|---|---|---|
| 文本API | | | |
| 图像API | | | |
| BLIP→文本模型 | | | |
| 本地部署（选做） | | | |
| SenseVoice | | | |
| CosyVoice2 | | | |
| 摄像头与集成（选做） | | | |

## 问题与复测

原始报错是什么？问题在哪个模块？改了哪里？复测后是否解决？

## 结果分析

写出一个正确回答和一个失败/不确定的例子，并说明判断依据。API调用成功不等于内容正确。

提交前删除密钥、个人路径，以及未经允许公开的图像和录音。依赖版本用 `python -m pip freeze` 保存为单独文件；不要直接上传含访问凭据的包地址。
